// Initial state
const state = {

}

// Getter
const getters = {

}

const actions = {
  // toBannerStore ({commit}, result) {
  //   commit('GET_BANNER', result)
  // }
}

// Mutations
const mutations = {
  // GET_BANNER (state, result) {
  //   state.banners = result
  // }
}

export default {
  state,
  getters,
  actions,
  mutations
}
